package com.example.nicestartagain;

    <?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NiceStartAgain">

        <!-- CORREGIDO: Esta es la ÚNICA Activity de inicio (LAUNCHER) -->
        <activity
            android:name=".Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- CORREGIDO: Se ha eliminado el intent-filter de LAUNCHER de esta Activity -->
        <activity
            android:name=".Login"
            android:exported="true"
            android:theme="@style/Theme.NiceStartAgain.NoActionBar"
            android:launchMode="singleTask" />

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.NiceStartAgain.WithActionBar"
            android:launchMode="singleTop" />

        <activity
            android:name=".Signup"
            android:exported="false"
            android:theme="@style/Theme.NiceStartAgain.WithActionBar" />
    </application>

</manifest>
